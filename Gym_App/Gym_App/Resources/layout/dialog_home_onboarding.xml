<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:background="@drawable/bg_card"
    android:paddingStart="24dp"
    android:paddingTop="22dp"
    android:paddingEnd="24dp"
    android:paddingBottom="20dp">

    <TextView
        android:id="@+id/onboardingTitleText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/home_onboarding_title"
        android:textColor="@color/color_text_primary"
        android:textStyle="bold"
        android:textSize="30sp"
        android:maxLines="2"
        android:breakStrategy="balanced"
        android:autoSizeTextType="uniform"
        android:autoSizeMinTextSize="24sp"
        android:autoSizeMaxTextSize="34sp"
        android:autoSizeStepGranularity="1sp"
        android:lineSpacingExtra="2dp" />

    <TextView
        android:id="@+id/onboardingMessageText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:text="@string/home_onboarding_message"
        android:textColor="@color/color_text_secondary"
        android:textSize="18sp"
        android:lineSpacingExtra="6dp" />

    <Button
        android:id="@+id/onboardingCtaButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:minHeight="@dimen/gym_primary_button_height"
        android:text="@string/home_onboarding_cta"
        android:textAllCaps="false"
        android:textStyle="bold"
        android:textColor="@android:color/black"
        android:background="@drawable/bg_button_primary"
        android:stateListAnimator="@animator/edit_button_elevation"
        android:elevation="4dp"
        android:fontFamily="sans-serif" />

</LinearLayout>